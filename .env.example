# Vite development proxy target.
# Normal local Laravel server:
VITE_API_TARGET=http://127.0.0.1:8000

# For destructive smoke testing against form_builder_test, copy this file to
# .env.local and point at the Laravel testing server instead:
# VITE_API_TARGET=http://127.0.0.1:8001
